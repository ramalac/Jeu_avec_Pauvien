using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class startRules : MonoBehaviour
{
    private GameObject reglesPanel;

    // Start is called before the first frame update
    void Start()
    {
        //reglesPanel = transform.Find("ReglesPanel").gameObject;
        //reglesPanel.SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        /*if (Input.GetButtonDown("A"))
        {
            reglesPanel.SetActive(false);
        }*/
    }
}
